Thank you for installing {{ .Chart.Name }}!

Your release is named: {{ .Release.Name }}

To get more information about your release, run:

  helm status {{ .Release.Name }}
  helm get all {{ .Release.Name }}

To check the status of your pods:

  kubectl get pods -n {{ .Values.namespace.name }}

To check your services:

  kubectl get svc -n {{ .Values.namespace.name }}

To check your ingress:

  kubectl get ingress -n {{ .Values.namespace.name }}

ACCESS INFORMATION:

  Frontend URL: http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}/
  Backend API:  http{{ if .Values.ingress.tls.enabled }}s{{ end }}://{{ .Values.ingress.host }}/api

For DNS configuration, add to /etc/hosts:

  <INGRESS_IP> {{ .Values.ingress.host }}

Replace <INGRESS_IP> with your ingress-nginx external IP:

  kubectl get svc ingress-nginx-controller -n ingress-nginx

Happy K8sing! 🚀
